# Application
spring.application.name=reviewflow-backend
spring.profiles.active=local

# Database - use application-local.properties for actual connection (gitignored)
spring.datasource.url=jdbc:mysql://localhost:3306/reviewflow_dev
spring.datasource.username=root
spring.datasource.password=olamide588
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true

# Actuator
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=when-authorized

# Server
server.port=8081

# JWT (use env in production)
jwt.secret=b8WT4MrliZAVEjznsLwxy0qDAcwtMuoMrCapxOgs3G0=
jwt.access-expiration-ms=900000
jwt.refresh-expiration-ms=604800000

# CORS (comma-separated origins; use env in production)
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:8080}

# Cookie (Secure in production)
app.cookie.secure=${COOKIE_SECURE:false}
